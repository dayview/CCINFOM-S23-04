package businesspermitsystem.db;

import businesspermitsystem.models.InspectionScheduleModel;

/**
 * Data Access Object (DAO) for the {@link InspectionScheduleModel}.
 * 
 * This class handles all database interactions related to inspector schedules
 * including creating, reading, updating, and deleting records.
 * 
 * It uses {@link DatabaseConnector} to establish connections.
 */
public class InspectionScheduleDAO {
    
}
